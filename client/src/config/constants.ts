import { linkTypes } from "./types";

const homeUrl: string = '/';

// nav itmes
export const navItems: linkTypes[] = [
    {
        _id: "nav_1",
        title: "Home",
        url: homeUrl,
    },
];